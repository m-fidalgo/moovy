{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "evak",
  "database": "moovy",
  "synchronize": true,
  "migrations": ["./src/database/migrations/*.ts"],
  "entities": ["./src/models/{*.ts,*.js}"],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
